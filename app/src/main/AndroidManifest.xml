<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Urtisi"
        tools:targetApi="31">
        <activity
            android:name=".SettingsActivity"
            android:exported="false"/>

        <activity
            android:name=".Settings.ThemeSelectionActivity"
            android:exported="false"/>

        <activity android:name=".Settings.HelpActivity"
            android:exported="false"/>

        <activity android:name=".Settings.LinkActivity"
            android:exported="false"/>

        <activity android:name=".Menu.CallActivity"
            android:exported="false"/>

        <activity android:name=".Menu.EmployeesActivity"
            android:exported="false"/>

        <activity android:name=".Menu.InstituteActivity"
            android:exported="false"/>

        <activity android:name=".Menu.EducationActivity"
            android:exported="false"/>

        <activity android:name=".Menu.Education.EducationSpo1Activity"
            android:exported="false"/>

        <activity android:name=".Menu.Education.EducationSpo2Activity"
            android:exported="false"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Urtisi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>